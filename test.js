const path = require("path");

//You give me the exact location of the project and its path
console.log(__dirname);

//It allows me to add a new file to the existing path
const imagepath = path.join(__dirname, "images")

console.log(__dirname);
